# Example environment variables
# Database connection used by the app (replace credentials/host as needed)
DATABASE_URL="postgres://postgres:postgres@localhost:5432/ledger?sslmode=disable"
# HTTP server port
PORT=8080

